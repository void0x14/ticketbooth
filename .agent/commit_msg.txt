fix(ux): Remove aggressive refresh and fix null last_episode_to_air

- search_result_row.py: Remove win.refresh call from _on_add_done
  This was causing dialog freeze and background "Loading content..."
  
- series_model.py: Add null check for last_episode_to_air
  TMDB sometimes returns null, causing TypeError

Content will refresh when user returns to main view via existing
_check_needs_refresh mechanism.
